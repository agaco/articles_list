const prefix = 'CONFIG';
const type = 'WORKER';

export default  {
  GET_ID_TOKEN_REQUEST: `${type}:${prefix}_GET_ID_TOKEN_REQUEST`,
  CALL_REFRESH_TOKEN: `${type}:${prefix}CALL_REFRESH_TOKEN`,
  GET_REFRESH_TOKEN_REQUEST: `${type}:${prefix}GET_REFRESH_TOKEN_REQUEST:`,
};

